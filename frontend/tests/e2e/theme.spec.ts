import { expect, test } from '@playwright/test'
test.beforeEach(async({page})=>{await page.addInitScript(()=>localStorage.setItem('misaka.access_token','test-token'));await page.route('**/api/v1/**',async route=>{const url=route.request().url();let data:any={};if(url.includes('/user/info'))data={email:'user@example.com',plan:{name:'标准套餐'},transfer_enable:536870912000,expired_at:1783700000};else if(url.includes('/getSubscribe'))data={u:10737418240,d:21474836480,transfer_enable:536870912000};else data=[];await route.fulfill({json:{status:'success',data}})});await page.goto('/#/dashboard')})
test('switches theme modes and persists selection',async({page})=>{await expect(page.getByRole('heading',{name:/欢迎回来/})).toBeVisible();await page.getByRole('button',{name:'外观主题'}).click();await page.getByRole('button',{name:'暗色'}).click();await expect(page.locator('html')).toHaveClass(/dark/);await expect.poll(()=>page.evaluate(()=>localStorage.getItem('misaka.theme-mode'))).toBe('dark');await page.reload();await expect(page.locator('html')).toHaveClass(/dark/)})
test('system mode responds to operating-system changes',async({page})=>{await page.emulateMedia({colorScheme:'dark'});await expect(page.locator('html')).toHaveClass(/dark/);await page.emulateMedia({colorScheme:'light'});await expect(page.locator('html')).not.toHaveClass(/dark/)})
test('has no horizontal overflow',async({page})=>{const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false)})
